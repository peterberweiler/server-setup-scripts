
# Server Setup Scripts
Don't use them they are bad.

```
sudo apt-get update
sudo apt-get install -y curl

curl -sSL https://raw.githubusercontent.com/peterberweiler/server-setup-scripts/main/other.sh | sudo sh

curl -sSL https://raw.githubusercontent.com/peterberweiler/server-setup-scripts/main/aliases.sh | sudo sh

curl -sSL https://raw.githubusercontent.com/peterberweiler/server-setup-scripts/main/ufw.sh | sudo sh

curl -sSL https://raw.githubusercontent.com/peterberweiler/server-setup-scripts/main/docker.sh | sudo sh

curl -sSL https://raw.githubusercontent.com/peterberweiler/server-setup-scripts/main/public_keys.sh | sudo sh

```